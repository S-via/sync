{
  "name": "server",
  "version": "1.0.0",
  "description": "to be in sync with them",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "silvia v",
  "license": "ISC",
  "dependencies": {
    "socket.io": "^4.8.1"
  }
}
